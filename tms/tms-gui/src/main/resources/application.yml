spring:
  datasource:
    username: ${TMS_DB_USERNAME}
    url: ${TMS_DB_URL}
    password: ${TMS_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate.format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: taskmanagementserviceteam@gmail.com
    password: sormvbtcaldrbltc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

logging:
  level:
    root: info
    pu.master: debug

jwt:
  secret: f232@Gg2f32HJ*_56538fj2W@FJ@fmf9F@2ri92IT2tjf$@@fev

application:
  admin:
    username: ${TMS.ADMIN.USERNAME}
    password: ${TMS.ADMIN.PASSWORD}

vaadin:
  launch-browser: true